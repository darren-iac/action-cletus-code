name: Final Verification Test

# This workflow triggers review-workflows.yml to verify
# that only ONE bot comment is posted per PR review cycle.

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Final verification test - triggers review-workflows.yml"
